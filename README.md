# tauri-practice

## テスト

- **単体・統合テスト**: `pnpm test`（Vitest）
- **ウォッチ**: `pnpm test:watch`
- **カバレッジ**: `pnpm test:coverage`
- **E2E**: `pnpm test:e2e`（Playwright）。初回または Playwright 更新後は `pnpm exec playwright install` でブラウザをインストールしてください。
